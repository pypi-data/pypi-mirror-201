[project]
name = "CompressedFisher"
authors = [
    {name = "William Coulton"},
]
readme = "README.rst"

requires-python = ">=3.5"
dynamic = ["version", "description"]

dependencies = [
    "numpy",
    "scipy"
]