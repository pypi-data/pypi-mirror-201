[tool.poetry]
name = "ruspie"
version = "0.1.0"
description = ""
authors = ["Factly Labs"]
readme = "README.md"
packages = [
    { include = "temp" },
]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
