[tool.poetry]
name = "dirmarks"
version = "0.1.0"
description = "Bookmarks for the filesystem"
authors = ["Meir Michanie <meirm@riunx.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
markdir = "dirmarks.main:main"

