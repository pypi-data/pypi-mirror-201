[tool.poetry]
name = "3d"
version = "0.1.1"
description = "3D makes 3D game design easy"
authors = ["dylanopen <opendylan@proton.me>"]
readme = "README.md"

packages = [
	{ include = "g" }
]

[tool.poetry.dependencies]
python = "^3.10"



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
