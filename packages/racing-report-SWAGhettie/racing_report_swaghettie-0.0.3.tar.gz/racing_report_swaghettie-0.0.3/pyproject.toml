[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
sources = ["src"]

[project]
name = "racing_report_SWAGhettie"
version = "0.0.3"
authors = [
  { name="Ihor Prokopenko", email="scaryscreamerg@gmail.com" },
]
description = "App that sorts racing results and store it in logs"
readme = "README.md"
requires-python = ">=3.11.0"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = 'https://git.foxminded.ua/foxstudent104424/task-6-report-of-monaco-2018-racing/-/merge_requests'
