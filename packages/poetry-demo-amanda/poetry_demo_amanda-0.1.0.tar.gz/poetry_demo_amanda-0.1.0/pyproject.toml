[tool.poetry]
name = "poetry-demo-Amanda"
version = "0.1.0"
description = ""
authors = ["Amanda <z4ghjdyx@foxmail.com>"]
readme = "README.md"
packages = [{include = "poetry_demo"}]

[tool.poetry.dependencies]
python = "^3.9"
pendulum = "^2.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
