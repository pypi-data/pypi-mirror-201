"""
    VRt.Universal [UV]

    The version of the OpenAPI document: 6.4.1962
    Contact: servicedesk@veeroute.com
    Auto Generated
"""


import re  # noqa: F401
import sys  # noqa: F401

from vrt_lss_universal.model_utils import (  # noqa: F401
    ApiTypeError,
    ModelComposed,
    ModelNormal,
    ModelSimple,
    cached_property,
    change_keys_js_to_python,
    convert_js_args_to_python_args,
    date,
    datetime,
    file_type,
    none_type,
    validate_get_composed_info,
    OpenApiModel
)
from vrt_lss_universal.exceptions import ApiAttributeError


def lazy_import():
    from vrt_lss_universal.model.attributes import Attributes
    from vrt_lss_universal.model.performer_tariff import PerformerTariff
    from vrt_lss_universal.model.time_window import TimeWindow
    from vrt_lss_universal.model.work_and_rest_rules import WorkAndRestRules
    globals()['Attributes'] = Attributes
    globals()['PerformerTariff'] = PerformerTariff
    globals()['TimeWindow'] = TimeWindow
    globals()['WorkAndRestRules'] = WorkAndRestRules


class PerformerShift(ModelNormal):
    """NOTE: This class is auto generated by OpenAPI Generator.

    Do not edit the class manually.

    Attributes:
      allowed_values (dict): The key is the tuple path to the attribute
          and the for var_name this is (var_name,). The value is a dict
          with a capitalized key describing the allowed value and an allowed
          value. These dicts store the allowed enum values.
      attribute_map (dict): The key is attribute name
          and the value is json key in definition.
      discriminator_value_class_map (dict): A dict to go from the discriminator
          variable value to the discriminator class name.
      validations (dict): The key is the tuple path to the attribute
          and the for var_name this is (var_name,). The value is a dict
          that stores validations for max_length, min_length, max_items,
          min_items, exclusive_maximum, inclusive_maximum, exclusive_minimum,
          inclusive_minimum, and regex.
      additional_properties_type (tuple): A tuple of classes accepted
          as additional properties values.
    """

    allowed_values = {
    }

    validations = {
        ('key',): {
            'max_length': 1024,
            'min_length': 1,
        },
        ('start_location_key',): {
            'max_length': 1024,
            'min_length': 1,
        },
        ('finish_location_key',): {
            'max_length': 1024,
            'min_length': 1,
        },
        ('max_locations',): {
            'inclusive_maximum': 1000,
            'inclusive_minimum': 1,
        },
        ('max_stops',): {
            'inclusive_maximum': 1000,
            'inclusive_minimum': 1,
        },
    }

    additional_properties_type = None

    _nullable = False

    @cached_property
    def openapi_types():
        """
        This must be a method because a model may have properties that are
        of type self, this must run after the class is loaded

        Returns
            openapi_types (dict): The key is attribute name
                and the value is attribute type.
        """
        lazy_import()
        return {
            'key': (str,),  
            'availability_time': (TimeWindow,),  
            'working_time': (TimeWindow,),  
            'start_location_key': (str, none_type,),  
            'finish_location_key': (str, none_type,),  
            'max_locations': (int, none_type,),  
            'max_stops': (int, none_type,),  
            'tariff': (PerformerTariff,),  
            'work_and_rest_rules': (WorkAndRestRules,),  
            'attributes': (Attributes,),  
        }

    @cached_property
    def discriminator():
        return None


    attribute_map = {
        'key': 'key',  
        'availability_time': 'availability_time',  
        'working_time': 'working_time',  
        'start_location_key': 'start_location_key',  
        'finish_location_key': 'finish_location_key',  
        'max_locations': 'max_locations',  
        'max_stops': 'max_stops',  
        'tariff': 'tariff',  
        'work_and_rest_rules': 'work_and_rest_rules',  
        'attributes': 'attributes',  
    }

    read_only_vars = {
    }

    _composed_schemas = {}

    @classmethod
    @convert_js_args_to_python_args
    def _from_openapi_data(cls, key, availability_time, working_time, *args, **kwargs):  
        """PerformerShift - a model defined in OpenAPI

        Args:
            key (str): Shift key, unique identifier.
            availability_time (TimeWindow):
            working_time (TimeWindow):

        Keyword Args:
            _check_type (bool): if True, values for parameters in openapi_types
                                will be type checked and a TypeError will be
                                raised if the wrong type is input.
                                Defaults to True
            _path_to_item (tuple/list): This is a list of keys or values to
                                drill down to the model in received_data
                                when deserializing a response
            _spec_property_naming (bool): True if the variable names in the input data
                                are serialized names, as specified in the OpenAPI document.
                                False if the variable names in the input data
                                are pythonic names, e.g. snake case (default)
            _configuration (Configuration): the instance to use when
                                deserializing a file_type parameter.
                                If passed, type conversion is attempted
                                If omitted no type conversion is done.
            _visited_composed_classes (tuple): This stores a tuple of
                                classes that we have traveled through so that
                                if we see that class again we will not use its
                                discriminator again.
                                When traveling through a discriminator, the
                                composed schema that is
                                is traveled through is added to this set.
                                For example if Animal has a discriminator
                                petType and we pass in "Dog", and the class Dog
                                allOf includes Animal, we move through Animal
                                once using the discriminator, and pick Dog.
                                Then in Dog, we will make an instance of the
                                Animal class but this time we won't travel
                                through its discriminator because we passed in
                                _visited_composed_classes = (Animal,)
            start_location_key (str, none_type): Start location key.  If not specified, the trip starts from the first order location. . [optional] if omitted the server will use the default value of "null"  
            finish_location_key (str, none_type): Finish location key.  If not specified, the trip finishes on the last order location. . [optional] if omitted the server will use the default value of "null"  
            max_locations (int, none_type): Limit of the number of unique locations in one trip. If not specified or null, number of locations is not limited. . [optional]  
            max_stops (int, none_type): Limit of the number of stops in one trip. If not specified or null, number of stops is not limited. . [optional]  
            tariff (PerformerTariff): [optional]  
            work_and_rest_rules (WorkAndRestRules): [optional]  
            attributes (Attributes): [optional]  
        """

        _check_type = kwargs.pop('_check_type', True)
        _spec_property_naming = kwargs.pop('_spec_property_naming', True)
        _path_to_item = kwargs.pop('_path_to_item', ())
        _configuration = kwargs.pop('_configuration', None)
        _visited_composed_classes = kwargs.pop('_visited_composed_classes', ())

        self = super(OpenApiModel, cls).__new__(cls)

        if args:
            for arg in args:
                if isinstance(arg, dict):
                    kwargs.update(arg)
                else:
                    raise ApiTypeError(
                        "Invalid positional arguments=%s passed to %s. Remove those invalid positional arguments." % (
                            args,
                            self.__class__.__name__,
                        ),
                        path_to_item=_path_to_item,
                        valid_classes=(self.__class__,),
                    )

        self._data_store = {}
        self._check_type = _check_type
        self._spec_property_naming = _spec_property_naming
        self._path_to_item = _path_to_item
        self._configuration = _configuration
        self._visited_composed_classes = _visited_composed_classes + (self.__class__,)

        self.key = key
        self.availability_time = availability_time
        self.working_time = working_time
        for var_name, var_value in kwargs.items():
            if var_name not in self.attribute_map and \
                        self._configuration is not None and \
                        self._configuration.discard_unknown_keys and \
                        self.additional_properties_type is None:
                # discard variable.
                continue
            setattr(self, var_name, var_value)
        return self

    required_properties = set([
        '_data_store',
        '_check_type',
        '_spec_property_naming',
        '_path_to_item',
        '_configuration',
        '_visited_composed_classes',
    ])

    @convert_js_args_to_python_args
    def __init__(self, key, availability_time, working_time, *args, **kwargs):  
        """PerformerShift - a model defined in OpenAPI

        Args:
            key (str): Shift key, unique identifier.
            availability_time (TimeWindow):
            working_time (TimeWindow):

        Keyword Args:
            _check_type (bool): if True, values for parameters in openapi_types
                                will be type checked and a TypeError will be
                                raised if the wrong type is input.
                                Defaults to True
            _path_to_item (tuple/list): This is a list of keys or values to
                                drill down to the model in received_data
                                when deserializing a response
            _spec_property_naming (bool): True if the variable names in the input data
                                are serialized names, as specified in the OpenAPI document.
                                False if the variable names in the input data
                                are pythonic names, e.g. snake case (default)
            _configuration (Configuration): the instance to use when
                                deserializing a file_type parameter.
                                If passed, type conversion is attempted
                                If omitted no type conversion is done.
            _visited_composed_classes (tuple): This stores a tuple of
                                classes that we have traveled through so that
                                if we see that class again we will not use its
                                discriminator again.
                                When traveling through a discriminator, the
                                composed schema that is
                                is traveled through is added to this set.
                                For example if Animal has a discriminator
                                petType and we pass in "Dog", and the class Dog
                                allOf includes Animal, we move through Animal
                                once using the discriminator, and pick Dog.
                                Then in Dog, we will make an instance of the
                                Animal class but this time we won't travel
                                through its discriminator because we passed in
                                _visited_composed_classes = (Animal,)
            start_location_key (str, none_type): Start location key.  If not specified, the trip starts from the first order location. . [optional] if omitted the server will use the default value of "null"  
            finish_location_key (str, none_type): Finish location key.  If not specified, the trip finishes on the last order location. . [optional] if omitted the server will use the default value of "null"  
            max_locations (int, none_type): Limit of the number of unique locations in one trip. If not specified or null, number of locations is not limited. . [optional]  
            max_stops (int, none_type): Limit of the number of stops in one trip. If not specified or null, number of stops is not limited. . [optional]  
            tariff (PerformerTariff): [optional]  
            work_and_rest_rules (WorkAndRestRules): [optional]  
            attributes (Attributes): [optional]  
        """

        _check_type = kwargs.pop('_check_type', True)
        _spec_property_naming = kwargs.pop('_spec_property_naming', False)
        _path_to_item = kwargs.pop('_path_to_item', ())
        _configuration = kwargs.pop('_configuration', None)
        _visited_composed_classes = kwargs.pop('_visited_composed_classes', ())

        if args:
            for arg in args:
                if isinstance(arg, dict):
                    kwargs.update(arg)
                else:
                    raise ApiTypeError(
                        "Invalid positional arguments=%s passed to %s. Remove those invalid positional arguments." % (
                            args,
                            self.__class__.__name__,
                        ),
                        path_to_item=_path_to_item,
                        valid_classes=(self.__class__,),
                    )

        self._data_store = {}
        self._check_type = _check_type
        self._spec_property_naming = _spec_property_naming
        self._path_to_item = _path_to_item
        self._configuration = _configuration
        self._visited_composed_classes = _visited_composed_classes + (self.__class__,)

        self.key = key
        self.availability_time = availability_time
        self.working_time = working_time
        for var_name, var_value in kwargs.items():
            if var_name not in self.attribute_map and \
                        self._configuration is not None and \
                        self._configuration.discard_unknown_keys and \
                        self.additional_properties_type is None:
                # discard variable.
                continue
            setattr(self, var_name, var_value)
            if var_name in self.read_only_vars:
                raise ApiAttributeError(f"`{var_name}` is a read-only attribute. Use `from_openapi_data` to instantiate "
                                     f"class with read only attributes.")
