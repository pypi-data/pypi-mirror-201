[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rawjson"
version = "0.1.7"
description = "/tellraw generator for Minecraft"
requires-python = ">=3.6"
readme = "README.md"