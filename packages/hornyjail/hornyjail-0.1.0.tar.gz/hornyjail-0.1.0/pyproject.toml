[tool.pdm]

[project]
name = "hornyjail"
version = "0.1.0"
description = "Nothing here for now"
authors = [
    { name = "ddnomad", email = "self@ddnomad.net" },
]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "Unlicense"

[build-system]
requires = [
    "pdm-pep517>=1.0",
]
build-backend = "pdm.pep517.api"
