[tool.poetry]
name = "gen-markdown"
version = "0.0.1"
description = ""
authors = ["Jordan Paoletti <jordanspaoletti@gmail.com>"]
readme = "README.md"
packages = [{include = "gen_markdown"}]
exclude = [
    "**/.pytest_cache/**/*"
]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.test.dependencies]
pytest = "^7.2.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
