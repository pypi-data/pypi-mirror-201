[build-system]
requires = [
    "setuptools >= 64",
    "setuptools_scm >= 7",
]
build-backend = "setuptools.build_meta"

[project]
name = "dispatches-synthetic-price-data" 
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "dispatches-data-packages >= 23.3.15.1",
]

[tool.setuptools_scm]
