---
title: "{{ cookiecutter.project_title }}"
format:
    hugo:
        toc: false
---


This is the website homepage generated by `index.qmd` under the root directory. For authoring with `.qmd` files, see Quarto's [website](https://quarto.org/).

Edit `index.qmd` and re-run `astrodown dev --render-quarto` to apply changes.

```{r}
#| message: false
#| echo: false
library(plotly)
fig <- plot_ly(midwest, x = ~percollege, color = ~state, type = "box")
fig
```



