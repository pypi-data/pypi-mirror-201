[tool.poetry]
name = "kickoff-epl"
version = "0.1.1"
description = ""
authors = ["Harshith <harshithl1777@gmail.com>"]
readme = "README.md"
packages = [{include = "kickoff_epl"}]

[tool.poetry.scripts]
kickoff = "kickoff_epl.main:app"

[tool.poetry.dependencies]
python = "^3.11"
typer = {extras = ["all"], version = "^0.7.0"}
numpy = "1.24.1"
pandas = "^2.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
