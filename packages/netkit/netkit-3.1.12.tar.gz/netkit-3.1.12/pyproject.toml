[build-system]
requires = ["setuptools>=61.2"]
build-backend = "setuptools.build_meta"

[project]
name = "netkit"
version = "3.1.12"
authors = [{name = "dantezhu", email = "zny2008@gmail.com"}]
license = {text = "MIT"}
description = "useful kit for network programming"
urls = {Homepage = "https://github.com/dantezhu/netkit"}
requires-python = ">=3"
dependencies = []

[tool.setuptools]
script-files = []
include-package-data = false

[tool.setuptools.packages.find]
exclude = ["ez_setup"] # examples; tests
namespaces = false
