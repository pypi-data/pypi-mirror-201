[tool.poetry]
name = "yaml-setup"
version = "0.1.1"
description = ""
authors = ["arnos-stuff <bcda0276@gmail.com>"]
readme = "README.md"
packages = [{include = "yamlup"}]

[tool.poetry.dependencies]
python = "^3.9"
pyyaml = "^6.0"
typer = {extras = ["all"], version = "^0.7.0"}
schema = "^0.7.5"

[tool.poetry.scripts]
yamlup = "yamlup.cli:app"
ymlsetup = "yamlup.cli:app"
yaml-setup = "yamlup.cli:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
