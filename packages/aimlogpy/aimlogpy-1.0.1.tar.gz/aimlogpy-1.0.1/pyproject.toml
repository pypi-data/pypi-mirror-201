[project]
name = "aimlogpy"
description = "This is a Python Package to generate logs in a local file y the root of the app where is used."
version = "1.0.1"
authors = [
    { name = "Mia Nicole Ruiz Greco", email = "miarg49@gmail.com" }
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest"
]

[build-system]