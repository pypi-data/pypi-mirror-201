[tool.poetry]
name = "image-markup"
version = "0.1.0"
description = ""
authors = ["Dan <kelleherjdan@gmail.com>"]
readme = "README.md"
packages = [{include = "image_markup"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
