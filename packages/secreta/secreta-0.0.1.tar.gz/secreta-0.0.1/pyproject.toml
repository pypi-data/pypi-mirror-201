[tool.poetry]
name = "secreta"
version = "0.0.1"
description = ""
authors = ["caiopeternela <caiopeternela.dev@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
secreta = "secreta.commands:app"

[tool.ruff]
line-length = 120

[tool.isort]
line_length = 120

[tool.poetry.dependencies]
python = "^3.8"
typer = "^0.7.0"
cryptography = "^40.0.1"

[tool.poetry.group.dev.dependencies]
ruff = "^0.0.259"
isort = "^5.12.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
