[build-system]
requires = [
    "wheel",
    "setuptools>=45",
    "setuptools_scm[toml]>=6.0",
    "ninja",
    "cmake>=3.18.2",
    "cmake-build-extension",
]
build-backend = "setuptools.build_meta"
