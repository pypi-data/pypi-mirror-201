[tool.poetry]
name = "django-client-side-templates"
version = "0.2.0"
description = "Create DOM elements on the fly. Use Django templates from JavaScript."
authors = ["Josiah Kaviani <proofit404@gmail.com>"]
license = "BSD-2-Clause"
readme = "README.md"
packages = [{include = "django_client_side_templates"}]

[tool.poetry.dependencies]
python = "*"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
