# pyproject.toml

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "qfinance"
version = "1.0.3"
description = "Quantum Finance Library"
readme = "README.md"
license = {file = "LICENSE"}

dependencies = [
    "qcompute >= 3.3.0"
]

requires-python = ">=3.9"